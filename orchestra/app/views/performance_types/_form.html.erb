<%= form_for(@performance_type) do |f| %>
  <% if @performance_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@performance_type.errors.count, "error") %> prohibited this performance_type from being saved:</h2>

      <ul>
      <% @performance_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
